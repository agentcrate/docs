allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver

# Pin transitive postcss past GHSA-qx2v-qp2m-jg93 (XSS via unescaped </style>
# in CSS stringify output, fixed in 8.5.10). next@16.2.6 still resolves
# postcss@8.4.31 transitively; force-bump until next ships a release that
# moves off it.
overrides:
  postcss: "^8.5.15"
